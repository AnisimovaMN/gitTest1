 оманды программы Git

git --version показывает текущую версию программы

git init инициализаци€ программы дл€ отслеживани€ изменений,создание репозитори€

git status показывает статус происход€щего в папке, какие файлы добавлены

git add <им€_файла> добавл€ет файл

git commit -m " омментарий изменени€" фиксирует текущий статус и комментарий

git log журнал изменений

git checkout <название ветки> переход на другую версию файла

git diff показывает разницу между текущим состо€нием и тем что уже сохранено

git branch <название ветки> создание новой ветки

git merge <название ветки> сли€ние веток

git branch -d <название ветки> удаление ветки

git branch images создание ветки картинки

git log --graph показывает сколько веток (дерево коммитов)

git help подсказки по командам 

clear очистка пол€ терминала
